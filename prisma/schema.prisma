generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// Simple independent site template - all auth/payment/subscription tables removed
// Add your own domain-specific models here as needed